# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
event-times-local-backup*.json
*-backup.json

# Environment variables
.env
.env.local
.env.*.local

# Local project notes for Codex only
CODEX_INSTRUCTIONS.md
EVENT_TIMES_SPEC.md
EVENT_TIMES_UI_RULES.md

# Local Claude Code settings (machine-specific permissions)
.claude/settings.local.json

# Local Claude Code usage readings (plan-limit guard state — machine-specific, transient)
.claude/usage-state.json
.claude/usage-guard-state.json
.claude/*.tmp
.claude/**/*.tmp
CLAUDE.local.md

# Local knowledge graph (rebuilt per-machine by the graphify post-commit hook)
graphify-out/

# Playwright CLI session output (screenshots, traces from UI QA runs)
.playwright-cli/

# docs/ vault — local-only knowledge base (public repo; contains business/security-sensitive content), not versioned in git
docs/

# Local working briefs pasted from external tools (third-party prompts/code)
tasks/references/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
