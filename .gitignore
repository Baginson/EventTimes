# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
event-times-local-backup*.json
*-backup.json

# Environment variables
.env
.env.local
.env.*.local

# Local project notes for Codex only
CODEX_INSTRUCTIONS.md
EVENT_TIMES_SPEC.md
EVENT_TIMES_UI_RULES.md
docs/design-references/

# Local Claude Code settings (machine-specific permissions)
.claude/settings.local.json
CLAUDE.local.md

# Local knowledge graph (rebuilt per-machine by the graphify post-commit hook)
graphify-out/

# Playwright CLI session output (screenshots, traces from UI QA runs)
.playwright-cli/

# Obsidian vault artifacts inside docs/ (user-local editor state and plugins)
docs/.obsidian/
docs/.smart-env/

# Local working briefs pasted from external tools (third-party prompts/code)
tasks/references/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
